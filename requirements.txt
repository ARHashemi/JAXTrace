# Core dependencies
numpy>=1.20.0
matplotlib>=3.3.0
seaborn>=0.11.0
vtk>=9.0.0
scipy>=1.7.0
psutil>=5.8.0

# JAX (CPU version by default, users can install GPU version separately)
jax>=0.4.0
jaxlib>=0.4.0

# Optional but recommended
plotly>=5.0.0
ipython>=7.0.0

# For GPU memory monitoring (optional)
pynvml>=11.0.0